'use strict';

define([''], function () {
  //*******************************************************************************************************************
  // ** Configuration
  //*******************************************************************************************************************
  var config = {};

  config.loadAudio = true;
  config.soundVolume = 0.4;
  config.soundtrackVolume = 0.2;

  config.tips = true;
  config.cheats = false;

  config.logos = [{ type: 'Image', source: 'logo', link: '' }];

  return config;
});